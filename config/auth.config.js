/**
 * export the auth config.
 * To be changed to a sophisticated auth in the future
 */
module.exports = {
    users: {
        'admin': 'admin',
        'user': 'pass',
        'test': 'test',
    }
}